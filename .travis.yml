language: node_js
env:
  - NODE_PATH=$(which node)
install:
  - yarn install
  - cd human-connection-api
  - yarn install
  - cd ..
services:
  - mongodb
node_js:
  - "9"
before_script:
  - cp local.travis.json human-connection-api/config/local.json
script:
  - yarn lint
  - yarn test
